#include "main.h"
/**
 * mul - prints the product of two integers
 * @a: first integer
 * @b: second integer
(*
 * Return: the product of the two integers
 */
int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
