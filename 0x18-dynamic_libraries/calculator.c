#include <stdio.h>

/**
 * add - add two numbers
 * @a: num to be added
 * @b: num to be added
 *
 */
add(a, b)
{
    return a + b;
}

/**
 * sub - add two numbers
 * @a: num to be subtracted
 * @b: num to be subtracted
 *
 */
sub(a, b)
{
    return a - b;
}

/**
 * mul- add two numbers
 * @a: num to be multiplied
 * @b: num to be multiplied
 *
 */
mul(a, b)
{
    return a * b;
}

/**
 * div - add two numbers
 * @a: num to be divided
 * @b: num to be divided
 *
 */
div(a, b)
{
    return a / b;
}

/**
 * mod - add two numbers
 * @a: num whose modulus is to be found
 * @b: num to whose modulus is to be cal
 *
 */
mod(a, b)
{
    return a % b;
}
